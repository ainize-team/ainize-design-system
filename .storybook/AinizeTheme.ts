import { create } from '@storybook/theming';
import AinizeLogo from './ainize-logo.svg';

export default create({
  base: 'dark',

  brandTitle: 'Ainize Design System',
  brandUrl: 'https://ainize.ai',
  brandImage: AinizeLogo,
});
