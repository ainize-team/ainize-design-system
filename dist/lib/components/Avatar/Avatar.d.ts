/// <reference types="react" />
export interface AvatarProps {
    src: string;
    width: number;
    height: number;
    css?: any;
}
export declare const Avatar: ({ src, width, height, css }: AvatarProps) => JSX.Element;
